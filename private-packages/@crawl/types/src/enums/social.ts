enum SocialProviderType {
    Kakao = 'kakao',
    Google = 'google',
    Apple = 'apple',
}

enum JoinProgressType {
    DONE = 'DONE',
    REGISTER0 = 'REGISTER0',
}

export { SocialProviderType, JoinProgressType };
