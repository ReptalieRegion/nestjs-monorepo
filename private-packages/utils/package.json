{
    "name": "@private-crawl/utils",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "prebuild": "rimraf dist",
        "build": "yarn prebuild && tsc -p tsconfig.build.json"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "5.0.4"
    },
    "packageManager": "yarn@4.1.0"
}
